name: location_tracker
description: 위치정보 자동 수집 앱
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

  # 위치 수집
  geolocator: ^11.0.0
  flutter_background_service: ^5.0.5
  flutter_background_service_android: ^6.2.2
  permission_handler: ^11.3.0

  # 포그라운드 서비스 알림 채널을 미리 명시적으로 만들기 위함
  # (채널이 준비되기 전에 startForeground가 호출되면 "Bad notification" 크래시가 남)
  flutter_local_notifications: ^17.2.2

  # 로컬 저장
  sqflite: ^2.3.2
  path_provider: ^2.1.2
  path: ^1.9.0

  # 네트워크 (카카오 API 호출용)
  http: ^1.2.0

  # 지도 웹뷰
  webview_flutter: ^4.7.0

  # 날짜 포맷 / 그룹핑
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
